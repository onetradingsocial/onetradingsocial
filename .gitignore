__pycache__/
.claude/
.agents/
.claire/
.mcp.json
skills-lock.json

seed-users.md
# The script itself, not just the notes — it carries plaintext passwords for
# accounts that are live in production, and this repo is public.
scripts/seed-users.mjs
scripts/seed-activity.log
backups/
# 231KB dump holding real staff addresses; regenerable, never worth committing.
dev-bootstrap.sql

# Secrets — never commit. .env.example is the only tracked env file.
.env
.env.*
!.env.example

app/test-output.txt

# n8n runtime output — the content queue itself IS tracked.
automation/n8n/post-log.jsonl
# Holds a live Instagram long-lived access token.
automation/n8n/instagram-token.json
# Watchdog and n8n console output.
automation/n8n/watchdog.log
automation/n8n/n8n-console.log
automation/n8n/n8n-console.err.log
# Rotated generations of the three append-only logs above (audit item 19 F7 —
# watchdog.ps1 now moves a file past 1 MB to <name>.1 rather than letting it
# grow without bound).
automation/n8n/*.log.1
automation/n8n/post-log.jsonl.1
