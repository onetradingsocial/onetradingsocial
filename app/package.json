{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "test": "vitest run",
    "e2e:preflight": "node scripts/e2e-preflight.mjs",
    "test:e2e": "playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "//engines": "Kept at the major on purpose — Vercel rejects engines ranges finer than 'NN.x'. .nvmrc pins 22.11.0. THAT PIN IS DISPUTED, do not treat it as a requirement without re-testing: it was added because the dev server appeared to die with 'controller[kState].transformAlgorithm is not a function' just before React's $RC reveal, hanging hard page loads on the Suspense skeleton. That has never reproduced as version-dependent. docs/qa-sweep-2026-08-21.md retracts it (the frozen page was a permanently-hidden preview tab: Chrome runs no rAF there and React 19.2 defers the Suspense reveal through rAF, so every $RC page looks stuck while the HTML is byte-perfect), and it was re-checked on 22.19.0 in the B0 follow-up with /login, /signup, /demo and /leaderboard all completing. 22.11.0 also cannot run vitest — require(esm) landed in 22.12. Raising the pin to >= 22.12 is the open item. See app/tests/e2e/README.md.",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.0",
    "@supabase/supabase-js": "^2.108.2",
    "@vercel/analytics": "^2.0.1",
    "ccxt": "^4.5.68",
    "next": "^15.5.19",
    "protobufjs": "^7.6.5",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "sanitize-html": "^2.17.5",
    "stripe": "^22.3.0",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.3.7",
    "@playwright/test": "^1.61.0",
    "@tailwindcss/postcss": "^4.3.1",
    "@types/node": "^25.9.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@types/sanitize-html": "^2.16.1",
    "@vitejs/plugin-react": "^6.0.2",
    "eslint": "^9.39.5",
    "eslint-config-next": "^15.5.25",
    "postcss": "^8.5.15",
    "tailwindcss": "^4.3.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
